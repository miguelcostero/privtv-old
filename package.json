{
  "name": "privtv",
  "version": "0.0.1",
  "description": "Alquiler de peliculas en streaming por suscripcion mensual",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --platform win32 --arch all --out dist/ --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "peliculas",
    "suscripcion",
    "streaming"
  ],
  "author": "Priv.TV",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^7.0.1",
    "electron-prebuilt": "^1.0.1",
    "electron-windows-installer": "^1.3.3"
  },
  "dependencies": {
    "request": "^2.72.0",
    "srt-to-vtt": "^1.1.1"
  }
}
